import 'package:flutter/material.dart';

class BannerManagement extends StatelessWidget {
  const BannerManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Banner Management'),);
  }
}